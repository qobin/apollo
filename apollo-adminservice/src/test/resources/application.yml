spring:
  application:
    name: apollo-adminservice

server:
  port: ${port:8090}

logging:
  level:
    org.springframework.cloud: 'DEBUG'
  file: ${HOME}/logs/apollo/${ctrip.appid}/apollo-adminservice.log

ctrip:
  appid: 100003172
